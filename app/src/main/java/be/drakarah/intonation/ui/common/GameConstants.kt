package be.drakarah.intonation.ui.common

/** Visual count-in shown before the first prompt of every exercise (note accuracy, sustain,
 * shift, chords). Lived in the note-accuracy package back when it was the only game; now shared. */
const val COUNT_IN_SECS = 5
